on:
  push:
    branches: [ actions-test ]

jobs:
  deploy:
    runs-on: self-hosted       
    steps:
      - uses: actions/checkout@v2
      - name: deploy to homelab
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.IP }}
          username: ${{ secrets.USERNAME }}
          key: ${{ secrets.KEY }}
          script: cd /opt/ && kill -9 $(pgrep python) && rm -rf vGate && git clone https://github.com/mm3906078/vGate.git && cd vGate 
